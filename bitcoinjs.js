const {
    Network,
    networks,
    Transaction,
    TxInput,
    TxOutput,
    address,
    script,
    payments,
    Block,
} = require('bitcoinjs-lib');

const { Buffer } = require('buffer');

module.exports = {
    Network,
    networks,
    Transaction,
    TxInput,
    TxOutput,
    address,
    script,
    payments,
    Block,
    Buffer,
};
